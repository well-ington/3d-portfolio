import React from 'react';
import {Canvas, useFrame, useThree, useLoader} from 'react-three-fiber';


const MeshLabs: React.FC = () => {
    return <Canvas style={{height: '100vh'}}>

        </Canvas>;
}

export default MeshLabs;